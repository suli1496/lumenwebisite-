/* ============================================================
   main.js — kleine interacties (mobiel menu openen/sluiten, enz.)
   (Nog leeg: we vullen dit in wanneer de header wordt gebouwd.)
   ============================================================ */
