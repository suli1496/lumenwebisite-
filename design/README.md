# Ontwerp-referenties — Lumen Security

- **hero-mixen-referentie.html** — de 4 hero-mixen (goede variant × variant F).
  - **MIX 1** = GEKOZEN → uitgebouwd als homepage-hero in `nl/index.html`.
  - **MIX 4** = bewaard als **template voor een latere donkere modus**
    (foto met navy-tint + rood-naar-zilver lichtbundel, lichte tekst).

Deze map bevat alleen ontwerp-referenties, geen live pagina's.
